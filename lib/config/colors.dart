import 'package:flutter/material.dart';

class AppColors {
  // Brand utama — hijau rempah/bumbu
  static const Color primary = Color(0xFF2E7D32);
  static const Color primaryLight = Color(0xFF4CAF50);
  static const Color primaryDark = Color(0xFF1B5E20);

  // Aksen
  static const Color accent = Color(0xFFFF8F00);
  static const Color accentLight = Color(0xFFFFB300);

  // Background
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0F4F0);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status Pesanan
  static const Color statusMenunggu = Color(0xFFFF9800);
  static const Color statusDiproses = Color(0xFF2196F3);
  static const Color statusSelesai = Color(0xFF4CAF50);
  static const Color statusDiserahkan = Color(0xFF9C27B0);

  // Status Transaksi
  static const Color transaksiPending = Color(0xFFFF9800);
  static const Color transaksiBerhasil = Color(0xFF4CAF50);
  static const Color transaksiGagal = Color(0xFFF44336);

  // Utility
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF1976D2);

  static const Color divider = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1A000000);
}
